[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18" # Node 16+ required for Prisma

[build.rollupOptions]
  external = ["tailwindcss"]